{
	"name": "Existing Docker Compose (Extend)",
	"dockerComposeFile": [
		"../docker-compose.yml",
		"../docker-compose.local.yml",
		"docker-compose.yml"
	],
	"service": "workspace",
	"workspaceFolder": "/workspace"
}
